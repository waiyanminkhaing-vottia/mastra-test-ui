import { ChatInterface } from '@/components/chat/chat-interface';

/**
 * Main home page component that displays the chat interface.
 */
export default function Home() {
  return <ChatInterface />;
}
